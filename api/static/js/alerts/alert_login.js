const errorMessage = document.getElementById('error-message');

if (errorMessage)
          console.log(error.Message.textContent); //muestra el mensaje de error en la consola
  //puedes utilizar este mensaje ´para mostrar una alerta o notificacion
